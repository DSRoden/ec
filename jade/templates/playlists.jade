html
  body
    ion-view(view-title='Playlists')
      ion-content
        ion-list
          ion-item(ng-repeat='playlist in playlists', href='#/endorsee/playlists/{{playlist.id}}')
            | {{playlist.title}}
